# Core Context

This directory contains shared context files that are used across all commands.

## Purpose

Place shared business context, stakeholder information, and other reference materials here that should be available to all Claude commands.

## Usage

Files in this directory can be referenced by any command in `.claude/commands/` to provide consistent context across different workflows.
